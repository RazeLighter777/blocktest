#include "position.h"

// Template method implementations must remain in the header file for Position<T> to be usable across translation units.
// No implementation to move to .cpp for templates unless explicit instantiation is required.
